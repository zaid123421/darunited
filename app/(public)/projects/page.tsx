import { PagePlaceholder } from "@/shared/components/ui/page-placeholder";

export default function PublicProjectsPage() {
  return (
    <PagePlaceholder
      title="Projects"
      description="Public portfolio — latest, featured, and most viewed projects."
    />
  );
}
